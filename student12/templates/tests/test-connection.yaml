apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "student12.fullname" . }}-test-connection"
  labels:
    {{- include "student12.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "student12.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
